name: esibil_call_flutter
description: eSIBiL prison video call app — Flutter cross-platform client.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.2.0
  shared_preferences: ^2.2.3
  http: ^1.2.1
  image_picker: ^1.1.2
  permission_handler: ^11.3.1
  provider: ^6.1.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
